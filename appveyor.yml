version: "{build}"

environment:
  nodejs_version: "10"

matrix:
  fast_finish: true

install:
  - choco install firefox
  - choco install googlechrome
  - ps: Install-Product node $env:nodejs_version
  - node --version
  - npm --version
  - npm install

test_script:
  - npm test
  - npm run test:karma
build: off